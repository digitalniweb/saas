require("@babel/register")({
	presets: [["babel-preset-env", { targets: { node: "current" } }]],
});
const path = require("path");
require("dotenv").config();

module.exports = {
	config: path.resolve("sequelize/server/config", "config.js"),
	"models-path": path.resolve("sequelize/server/models/apps"),
	"seeders-path": path.resolve("sequelize/server/seeders/apps"),
	"migrations-path": path.resolve("sequelize/server/migrations"),
};
